<UserControl x:Class="JungleControls.StatCounterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             x:ClassModifier="internal">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="{Binding HeaderRow}" Text="{Binding Control.Header}" FontSize="{Binding Control.HeaderFontSize}" Foreground="{Binding Control.HeaderForeground}" HorizontalAlignment="{Binding Control.HeaderAlignment}" FontWeight="{Binding Control.HeaderFontWeight}" Margin="{Binding HeaderMargin}" />
        <TextBlock Grid.Row="{Binding ContentRow}" Text="{Binding Content}" FontSize="{Binding Control.ContentFontSize}" Foreground="{Binding Control.ContentForeground}" HorizontalAlignment="{Binding Control.ContentAlignment}" FontWeight="{Binding Control.ContentFontWeight}" Margin="{Binding ContentMargin}" />
    </Grid>
</UserControl>
