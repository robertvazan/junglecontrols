<UserControl x:Class="TestApp.PropertySheetSampleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:jc="clr-namespace:JungleControls;assembly=JungleControls"
             xmlns:diagnostics="clr-namespace:System.Diagnostics;assembly=WindowsBase"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <jc:PropertySheet>
        <jc:PropertySheetRow Header="Label1">
            <TextBlock Text="Item1" />
        </jc:PropertySheetRow>
        <jc:PropertySheetRow Header="Input">
            <TextBox Text="{Binding Input, UpdateSourceTrigger=PropertyChanged}" />
        </jc:PropertySheetRow>
        <jc:PropertySheetRow Header="{Binding Input}">
            <TextBlock Text="{Binding Input}" />
        </jc:PropertySheetRow>
        <jc:PropertySheetRow Header="Otherlabel">
            <TextBlock Text="Item2 well something much longer" />
        </jc:PropertySheetRow>
        <TextBlock jc:PropertySheet.Header="Attached"  Text="{Binding Input}" />
    </jc:PropertySheet>
</UserControl>
