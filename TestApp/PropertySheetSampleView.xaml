<UserControl x:Class="TestApp.PropertySheetSampleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:jc="clr-namespace:JungleControls;assembly=JungleControls"
             xmlns:diagnostics="clr-namespace:System.Diagnostics;assembly=WindowsBase"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <jc:PropertySheet>
        <TextBlock jc:PropertySheet.Header="Label1" Text="Item1" />
        <TextBlock jc:PropertySheet.Header="Otherlabel" Text="Item2 well something much longer" />
        <TextBox jc:PropertySheet.Header="Input" Text="{Binding Input, UpdateSourceTrigger=PropertyChanged}" />
        <TextBlock jc:PropertySheet.Header="{Binding Input}" Text="{Binding Input}" />
    </jc:PropertySheet>
</UserControl>
